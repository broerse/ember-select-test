{{#if isEditing}}
	{{partial 'page-edit'}}
	<button {{action 'doneEditing'}}>Done</button>
{{else}}
	<button {{action 'edit'}}>Edit</button>
{{/if}}

<h1>{{title}}</h1>
<small class='muted'>Date: ({{format-date date}})</small>

<hr>

<div class='below-the-fold'>
	{{format-markdown body}}
</div>